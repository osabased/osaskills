interface:
  display_name: "Architecture Improvement"
  short_description: "Make one justified architecture improvement"
  default_prompt: "Use $architecture-improvement to complete one justified, bounded architecture improvement, including verification and cleanup. Return NO CHANGE when evidence does not support intervention, and respect any narrower requested scope."
policy:
  allow_implicit_invocation: false
