interface:
  display_name: "System Review"
  short_description: "Review cross-part behavior and failure paths"
  default_prompt: "Use $system-review to answer the system review question within the requested boundary. Return an evidence-backed verdict, material findings with closure conditions, and any deciding evidence gaps."
