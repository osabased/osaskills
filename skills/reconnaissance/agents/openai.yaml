interface:
  display_name: "Reconnaissance"
  short_description: "Delegate bounded evidence gathering to Luna"
  default_prompt: "Use $reconnaissance to gather only the missing evidence needed for the current decision. Prefer direct inspection for cheap lookups; otherwise dispatch the minimum independent GPT-5.6 Luna scouts and return compact, traceable evidence to the caller."
